# Shared Skills API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a TypeScript workspace prototype where one `pr-review` skill definition powers both an HTTP Registry API and an MCP server.

**Architecture:** Use npm workspaces with separate API and MCP apps plus shared `skill-spec` and `skill-runner` packages. Skills are file-backed under `skills/`, traces are file-backed under `.data/`, and Docker runs the API plus exposes a command path for the local MCP server.

**Tech Stack:** TypeScript 6, Node 22, npm workspaces, Express 5, Zod 4, YAML 2, Vitest 4, Supertest 7, stable `@modelcontextprotocol/sdk@1.29.0`.

---

## File Structure

- Create `package.json`: root workspace scripts and dependency versions.
- Create `package-lock.json`: generated by `npm install`.
- Create `tsconfig.base.json`: shared TypeScript compiler settings.
- Create `vitest.config.ts`: root test config.
- Create `apps/api/package.json`: API app package metadata.
- Create `apps/api/tsconfig.json`: API build config.
- Create `apps/api/src/app.ts`: Express app factory.
- Create `apps/api/src/index.ts`: API process entrypoint.
- Create `apps/api/test/api.test.ts`: HTTP integration tests.
- Create `apps/mcp-server/package.json`: MCP app package metadata.
- Create `apps/mcp-server/tsconfig.json`: MCP build config.
- Create `apps/mcp-server/src/server.ts`: MCP server factory and tool registration.
- Create `apps/mcp-server/src/index.ts`: stdio MCP process entrypoint.
- Create `apps/mcp-server/test/mcp-server.test.ts`: MCP smoke tests at the server-factory boundary.
- Create `packages/skill-spec/package.json`: shared schema package metadata.
- Create `packages/skill-spec/tsconfig.json`: schema package build config.
- Create `packages/skill-spec/src/index.ts`: manifest, input, output, request, and evaluation schemas.
- Create `packages/skill-spec/test/skill-spec.test.ts`: schema tests.
- Create `packages/skill-runner/package.json`: shared runner package metadata.
- Create `packages/skill-runner/tsconfig.json`: runner package build config.
- Create `packages/skill-runner/src/paths.ts`: root path resolution helpers.
- Create `packages/skill-runner/src/registry.ts`: file-backed skill registry.
- Create `packages/skill-runner/src/prReview.ts`: deterministic PR Review logic.
- Create `packages/skill-runner/src/runner.ts`: request validation, run orchestration, eval orchestration, and trace writing.
- Create `packages/skill-runner/src/index.ts`: package exports.
- Create `packages/skill-runner/test/registry.test.ts`: registry tests.
- Create `packages/skill-runner/test/runner.test.ts`: runner and PR Review tests.
- Create `skills/pr-review/skill.yaml`: first skill manifest.
- Create `skills/pr-review/instructions.md`: PR Review instructions.
- Create `skills/pr-review/examples/basic-risk.json`: first eval case.
- Create `skills/pr-review/examples/missing-tests.json`: second eval case.
- Create `Dockerfile`: local container image.
- Create `docker-compose.yml`: API service for local container smoke testing.
- Create `README.md`: local and Docker runbook.

## Task 1: Workspace Scaffold

**Files:**
- Create: `package.json`
- Create: `tsconfig.base.json`
- Create: `vitest.config.ts`
- Create: `apps/api/package.json`
- Create: `apps/api/tsconfig.json`
- Create: `apps/mcp-server/package.json`
- Create: `apps/mcp-server/tsconfig.json`
- Create: `packages/skill-spec/package.json`
- Create: `packages/skill-spec/tsconfig.json`
- Create: `packages/skill-runner/package.json`
- Create: `packages/skill-runner/tsconfig.json`
- Modify: `.gitignore`

- [ ] **Step 1: Create the root package manifest**

Create `package.json`:

```json
{
  "name": "llm-skills-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev:api": "tsx apps/api/src/index.ts",
    "dev:mcp": "tsx apps/mcp-server/src/index.ts",
    "start:api": "node apps/api/dist/index.js",
    "start:mcp": "node apps/mcp-server/dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "express": "5.2.1",
    "yaml": "2.9.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^22.15.0",
    "@types/supertest": "^6.0.3",
    "supertest": "7.2.2",
    "tsx": "4.22.4",
    "typescript": "6.0.3",
    "vitest": "4.1.8"
  }
}
```

- [ ] **Step 2: Create the shared TypeScript config**

Create `tsconfig.base.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "declaration": true,
    "sourceMap": true
  }
}
```

- [ ] **Step 3: Create the Vitest config**

Create `vitest.config.ts`:

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
    include: ["apps/**/*.test.ts", "packages/**/*.test.ts"],
    testTimeout: 10_000
  }
});
```

- [ ] **Step 4: Create workspace package manifests and tsconfigs**

Create `apps/api/package.json`:

```json
{
  "name": "@llm-skills-poc/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@llm-skills-poc/skill-runner": "0.1.0",
    "@llm-skills-poc/skill-spec": "0.1.0"
  }
}
```

Create `apps/api/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts"]
}
```

Create `apps/mcp-server/package.json`:

```json
{
  "name": "@llm-skills-poc/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@llm-skills-poc/skill-runner": "0.1.0",
    "@llm-skills-poc/skill-spec": "0.1.0"
  }
}
```

Create `apps/mcp-server/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts"]
}
```

Create `packages/skill-spec/package.json`:

```json
{
  "name": "@llm-skills-poc/skill-spec",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  }
}
```

Create `packages/skill-spec/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts"]
}
```

Create `packages/skill-runner/package.json`:

```json
{
  "name": "@llm-skills-poc/skill-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@llm-skills-poc/skill-spec": "0.1.0"
  }
}
```

Create `packages/skill-runner/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts"]
}
```

- [ ] **Step 5: Update ignored local output**

Ensure `.gitignore` contains these lines:

```gitignore
node_modules/
dist/
coverage/
.data/
.superpowers/
.DS_Store
```

- [ ] **Step 6: Install dependencies**

Run:

```bash
npm install
```

Expected: `package-lock.json` is created and workspace links are installed.

- [ ] **Step 7: Run baseline checks**

Run:

```bash
npm test -- --passWithNoTests
```

Expected: Vitest exits successfully even though no test files exist yet. Package builds start after source files exist in later tasks.

- [ ] **Step 8: Commit workspace scaffold**

Run:

```bash
git add .gitignore package.json package-lock.json tsconfig.base.json vitest.config.ts apps packages
git commit -m "chore: scaffold TypeScript workspace"
```

Expected: commit succeeds.

## Task 2: Skill Specification Schema

**Files:**
- Create: `packages/skill-spec/src/index.ts`
- Create: `packages/skill-spec/test/skill-spec.test.ts`

- [ ] **Step 1: Write failing schema tests**

Create `packages/skill-spec/test/skill-spec.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import {
  skillManifestSchema,
  skillRunRequestSchema,
  skillRunResultSchema
} from "../src/index.js";

describe("skill manifest schema", () => {
  it("accepts a valid PR review manifest", () => {
    const parsed = skillManifestSchema.parse({
      id: "pr-review",
      name: "Pull Request Review",
      version: "0.1.0",
      description: "Reviews code diffs for risk, correctness, and tests.",
      instructions: "instructions.md",
      inputs: [
        { name: "repo", type: "string", required: true },
        { name: "diff", type: "string", required: true },
        {
          name: "risk_level",
          type: "enum",
          required: true,
          values: ["low", "medium", "high"]
        }
      ],
      outputs: {
        type: "object",
        schema: {
          summary: "string",
          blocking_issues: "array",
          non_blocking_suggestions: "array",
          test_recommendations: "array",
          deployment_risk: "string"
        }
      },
      tools: [],
      guardrails: {
        require_citations: false,
        no_secret_exfiltration: true,
        confirm_before_write: true
      },
      targets: {
        openai: { expose_as_mcp_tool: true },
        claude: { package_as_skill: true },
        cursor: { expose_as_rule_and_mcp_tool: true }
      },
      examples: ["examples/basic-risk.json"]
    });

    expect(parsed.id).toBe("pr-review");
    expect(parsed.inputs).toHaveLength(3);
  });

  it("rejects enum inputs without values", () => {
    expect(() =>
      skillManifestSchema.parse({
        id: "bad-skill",
        name: "Bad Skill",
        version: "0.1.0",
        description: "Invalid skill.",
        instructions: "instructions.md",
        inputs: [{ name: "risk_level", type: "enum", required: true }],
        outputs: { type: "object", schema: { summary: "string" } }
      })
    ).toThrow();
  });
});

describe("skill run schemas", () => {
  it("accepts a run request", () => {
    const parsed = skillRunRequestSchema.parse({
      version: "0.1.0",
      client: "cursor",
      inputs: {
        repo: "checkout-service",
        diff: "diff --git a/file.ts b/file.ts",
        risk_level: "medium"
      }
    });

    expect(parsed.client).toBe("cursor");
  });

  it("accepts a structured PR review result", () => {
    const parsed = skillRunResultSchema.parse({
      skill_id: "pr-review",
      version: "0.1.0",
      result: {
        summary: "Change looks mostly safe.",
        blocking_issues: [],
        non_blocking_suggestions: ["Add focused tests."],
        test_recommendations: ["Run npm test."],
        deployment_risk: "medium"
      },
      trace_id: "trace_123"
    });

    expect(parsed.result.deployment_risk).toBe("medium");
  });
});
```

- [ ] **Step 2: Run schema tests and verify failure**

Run:

```bash
npm test -- packages/skill-spec/test/skill-spec.test.ts
```

Expected: FAIL because `packages/skill-spec/src/index.ts` does not exist.

- [ ] **Step 3: Implement schema exports**

Create `packages/skill-spec/src/index.ts`:

```ts
import { z } from "zod";

export const skillInputTypeSchema = z.enum(["string", "number", "boolean", "enum"]);

export const skillInputSchema = z
  .object({
    name: z.string().min(1),
    type: skillInputTypeSchema,
    required: z.boolean().default(false),
    values: z.array(z.string().min(1)).optional()
  })
  .superRefine((input, ctx) => {
    if (input.type === "enum" && (!input.values || input.values.length === 0)) {
      ctx.addIssue({
        code: z.ZodIssueCode.custom,
        message: "enum inputs must define at least one value",
        path: ["values"]
      });
    }
  });

export const outputPrimitiveSchema = z.enum(["string", "number", "boolean", "array", "object"]);

export const skillOutputSchema = z.object({
  type: z.literal("object"),
  schema: z.record(z.string(), outputPrimitiveSchema)
});

export const guardrailsSchema = z
  .object({
    require_citations: z.boolean().optional(),
    no_secret_exfiltration: z.boolean().optional(),
    confirm_before_write: z.boolean().optional()
  })
  .default({});

export const targetConfigSchema = z.record(
  z.string(),
  z.record(z.string(), z.union([z.string(), z.boolean(), z.number()]))
);

export const skillManifestSchema = z.object({
  id: z.string().regex(/^[a-z0-9][a-z0-9-]*$/),
  name: z.string().min(1),
  version: z.string().regex(/^\\d+\\.\\d+\\.\\d+$/),
  description: z.string().min(1),
  instructions: z.string().min(1),
  inputs: z.array(skillInputSchema).default([]),
  outputs: skillOutputSchema,
  tools: z.array(z.string()).default([]),
  guardrails: guardrailsSchema,
  targets: targetConfigSchema.default({}),
  examples: z.array(z.string()).default([])
});

export const skillRunRequestSchema = z.object({
  version: z.string().regex(/^\\d+\\.\\d+\\.\\d+$/).optional(),
  client: z.enum(["openai", "claude", "cursor", "internal", "api", "mcp"]),
  inputs: z.record(z.string(), z.unknown())
});

export const prReviewResultSchema = z.object({
  summary: z.string(),
  blocking_issues: z.array(z.string()),
  non_blocking_suggestions: z.array(z.string()),
  test_recommendations: z.array(z.string()),
  deployment_risk: z.enum(["low", "medium", "high"])
});

export const skillRunResultSchema = z.object({
  skill_id: z.string(),
  version: z.string(),
  result: prReviewResultSchema,
  trace_id: z.string()
});

export const skillEvaluationCaseSchema = z.object({
  name: z.string().min(1),
  inputs: z.record(z.string(), z.unknown()),
  expected: z.object({
    deployment_risk: z.enum(["low", "medium", "high"]).optional(),
    includes_suggestions: z.array(z.string()).default([]),
    includes_tests: z.array(z.string()).default([])
  })
});

export const skillEvaluationResultSchema = z.object({
  skill_id: z.string(),
  version: z.string(),
  passed: z.boolean(),
  cases: z.array(
    z.object({
      name: z.string(),
      passed: z.boolean(),
      failures: z.array(z.string())
    })
  )
});

export type SkillManifest = z.infer<typeof skillManifestSchema>;
export type SkillRunRequest = z.infer<typeof skillRunRequestSchema>;
export type PrReviewResult = z.infer<typeof prReviewResultSchema>;
export type SkillRunResult = z.infer<typeof skillRunResultSchema>;
export type SkillEvaluationCase = z.infer<typeof skillEvaluationCaseSchema>;
export type SkillEvaluationResult = z.infer<typeof skillEvaluationResultSchema>;
```

- [ ] **Step 4: Run schema tests and verify pass**

Run:

```bash
npm test -- packages/skill-spec/test/skill-spec.test.ts
```

Expected: PASS.

- [ ] **Step 5: Build schema package**

Run:

```bash
npm run build --workspace @llm-skills-poc/skill-spec
```

Expected: PASS and `packages/skill-spec/dist/index.js` is generated.

- [ ] **Step 6: Commit skill schema**

Run:

```bash
git add packages/skill-spec
git commit -m "feat: add shared skill spec schema"
```

Expected: commit succeeds.

## Task 3: File-Backed Skill Registry

**Files:**
- Create: `packages/skill-runner/src/paths.ts`
- Create: `packages/skill-runner/src/registry.ts`
- Create: `packages/skill-runner/src/index.ts`
- Create: `packages/skill-runner/test/registry.test.ts`

- [ ] **Step 1: Write failing registry tests**

Create `packages/skill-runner/test/registry.test.ts`:

```ts
import { mkdtemp, mkdir, writeFile } from "node:fs/promises";
import { join } from "node:path";
import { tmpdir } from "node:os";
import { describe, expect, it } from "vitest";
import { FileSkillRegistry } from "../src/registry.js";

async function createSkillFixture() {
  const root = await mkdtemp(join(tmpdir(), "skills-registry-"));
  const skillDir = join(root, "skills", "pr-review");
  await mkdir(join(skillDir, "examples"), { recursive: true });
  await writeFile(
    join(skillDir, "skill.yaml"),
    [
      "id: pr-review",
      "name: Pull Request Review",
      "version: 0.1.0",
      "description: Reviews code diffs.",
      "instructions: instructions.md",
      "inputs:",
      "  - name: repo",
      "    type: string",
      "    required: true",
      "outputs:",
      "  type: object",
      "  schema:",
      "    summary: string",
      "tools: []",
      "guardrails:",
      "  no_secret_exfiltration: true",
      "targets: {}",
      "examples:",
      "  - examples/basic-risk.json"
    ].join("\\n")
  );
  await writeFile(join(skillDir, "instructions.md"), "Review the diff.");
  await writeFile(
    join(skillDir, "examples", "basic-risk.json"),
    JSON.stringify({
      name: "basic risk",
      inputs: { repo: "demo", diff: "diff", risk_level: "low" },
      expected: { deployment_risk: "low" }
    })
  );
  return root;
}

describe("FileSkillRegistry", () => {
  it("lists validated skill metadata", async () => {
    const root = await createSkillFixture();
    const registry = new FileSkillRegistry({ rootDir: root });

    const skills = await registry.listSkills();

    expect(skills).toHaveLength(1);
    expect(skills[0]).toMatchObject({
      id: "pr-review",
      name: "Pull Request Review",
      version: "0.1.0"
    });
  });

  it("loads instructions and examples for one skill", async () => {
    const root = await createSkillFixture();
    const registry = new FileSkillRegistry({ rootDir: root });

    const skill = await registry.getSkill("pr-review");

    expect(skill.manifest.id).toBe("pr-review");
    expect(skill.instructions).toBe("Review the diff.");
    expect(skill.examples[0].name).toBe("basic risk");
  });

  it("rejects unknown skills", async () => {
    const root = await createSkillFixture();
    const registry = new FileSkillRegistry({ rootDir: root });

    await expect(registry.getSkill("missing")).rejects.toThrow("Skill not found: missing");
  });
});
```

- [ ] **Step 2: Run registry tests and verify failure**

Run:

```bash
npm test -- packages/skill-runner/test/registry.test.ts
```

Expected: FAIL because registry files do not exist.

- [ ] **Step 3: Implement path helpers**

Create `packages/skill-runner/src/paths.ts`:

```ts
import { dirname, resolve } from "node:path";
import { fileURLToPath } from "node:url";

const thisFile = fileURLToPath(import.meta.url);
const packageRoot = resolve(dirname(thisFile), "..", "..");

export function resolveProjectRoot(explicitRoot?: string): string {
  if (explicitRoot) {
    return resolve(explicitRoot);
  }
  if (process.env.SKILLS_PROJECT_ROOT) {
    return resolve(process.env.SKILLS_PROJECT_ROOT);
  }
  return resolve(packageRoot, "..", "..");
}

export function resolveSkillsDir(rootDir?: string): string {
  return resolve(resolveProjectRoot(rootDir), "skills");
}

export function resolveDataDir(rootDir?: string): string {
  return resolve(resolveProjectRoot(rootDir), ".data");
}
```

- [ ] **Step 4: Implement file-backed registry**

Create `packages/skill-runner/src/registry.ts`:

```ts
import { readdir, readFile } from "node:fs/promises";
import { join, resolve } from "node:path";
import YAML from "yaml";
import {
  type SkillEvaluationCase,
  type SkillManifest,
  skillEvaluationCaseSchema,
  skillManifestSchema
} from "@llm-skills-poc/skill-spec";
import { resolveSkillsDir } from "./paths.js";

export interface LoadedSkill {
  manifest: SkillManifest;
  instructions: string;
  examples: SkillEvaluationCase[];
  directory: string;
}

export interface FileSkillRegistryOptions {
  rootDir?: string;
  skillsDir?: string;
}

export class FileSkillRegistry {
  private readonly skillsDir: string;

  constructor(options: FileSkillRegistryOptions = {}) {
    this.skillsDir = options.skillsDir
      ? resolve(options.skillsDir)
      : resolveSkillsDir(options.rootDir);
  }

  async listSkills(): Promise<SkillManifest[]> {
    const entries = await readdir(this.skillsDir, { withFileTypes: true });
    const manifests = await Promise.all(
      entries
        .filter((entry) => entry.isDirectory())
        .map(async (entry) => this.readManifest(join(this.skillsDir, entry.name)))
    );
    return manifests.sort((left, right) => left.id.localeCompare(right.id));
  }

  async getSkill(skillId: string): Promise<LoadedSkill> {
    const directory = join(this.skillsDir, skillId);
    let manifest: SkillManifest;
    try {
      manifest = await this.readManifest(directory);
    } catch (error) {
      if (error instanceof Error && "code" in error && error.code === "ENOENT") {
        throw new Error(`Skill not found: ${skillId}`);
      }
      throw error;
    }

    if (manifest.id !== skillId) {
      throw new Error(`Skill id mismatch: requested ${skillId}, manifest declares ${manifest.id}`);
    }

    const instructions = await readFile(join(directory, manifest.instructions), "utf8");
    const examples = await Promise.all(
      manifest.examples.map(async (examplePath) => {
        const raw = await readFile(join(directory, examplePath), "utf8");
        return skillEvaluationCaseSchema.parse(JSON.parse(raw));
      })
    );

    return { manifest, instructions, examples, directory };
  }

  private async readManifest(directory: string): Promise<SkillManifest> {
    const raw = await readFile(join(directory, "skill.yaml"), "utf8");
    return skillManifestSchema.parse(YAML.parse(raw));
  }
}
```

- [ ] **Step 5: Export runner package modules**

Create `packages/skill-runner/src/index.ts`:

```ts
export * from "./paths.js";
export * from "./registry.js";
```

- [ ] **Step 6: Run registry tests and verify pass**

Run:

```bash
npm test -- packages/skill-runner/test/registry.test.ts
```

Expected: PASS.

- [ ] **Step 7: Build runner package**

Run:

```bash
npm run build --workspace @llm-skills-poc/skill-runner
```

Expected: PASS.

- [ ] **Step 8: Commit registry**

Run:

```bash
git add packages/skill-runner
git commit -m "feat: add file-backed skill registry"
```

Expected: commit succeeds.

## Task 4: PR Review Skill And Runner

**Files:**
- Create: `skills/pr-review/skill.yaml`
- Create: `skills/pr-review/instructions.md`
- Create: `skills/pr-review/examples/basic-risk.json`
- Create: `skills/pr-review/examples/missing-tests.json`
- Create: `packages/skill-runner/src/prReview.ts`
- Create: `packages/skill-runner/src/runner.ts`
- Modify: `packages/skill-runner/src/index.ts`
- Create: `packages/skill-runner/test/runner.test.ts`

- [ ] **Step 1: Write failing runner tests**

Create `packages/skill-runner/test/runner.test.ts`:

```ts
import { mkdtemp, mkdir, writeFile } from "node:fs/promises";
import { join } from "node:path";
import { tmpdir } from "node:os";
import { describe, expect, it } from "vitest";
import { FileSkillRegistry } from "../src/registry.js";
import { SkillRunner } from "../src/runner.js";

async function createProjectFixture() {
  const root = await mkdtemp(join(tmpdir(), "skills-runner-"));
  const skillDir = join(root, "skills", "pr-review");
  await mkdir(join(skillDir, "examples"), { recursive: true });
  await writeFile(
    join(skillDir, "skill.yaml"),
    [
      "id: pr-review",
      "name: Pull Request Review",
      "version: 0.1.0",
      "description: Reviews code changes for correctness, maintainability, tests, and risk.",
      "instructions: instructions.md",
      "inputs:",
      "  - name: repo",
      "    type: string",
      "    required: true",
      "  - name: diff",
      "    type: string",
      "    required: true",
      "  - name: risk_level",
      "    type: enum",
      "    required: true",
      "    values: [low, medium, high]",
      "outputs:",
      "  type: object",
      "  schema:",
      "    summary: string",
      "    blocking_issues: array",
      "    non_blocking_suggestions: array",
      "    test_recommendations: array",
      "    deployment_risk: string",
      "tools: []",
      "guardrails:",
      "  require_citations: false",
      "  no_secret_exfiltration: true",
      "  confirm_before_write: true",
      "targets: {}",
      "examples:",
      "  - examples/basic-risk.json"
    ].join("\\n")
  );
  await writeFile(join(skillDir, "instructions.md"), "Review code diffs as a senior engineer.");
  await writeFile(
    join(skillDir, "examples", "basic-risk.json"),
    JSON.stringify({
      name: "missing tests",
      inputs: {
        repo: "checkout",
        diff: "diff --git a/src/pay.ts b/src/pay.ts\\n+export function pay() { return charge(); }",
        risk_level: "medium"
      },
      expected: {
        deployment_risk: "medium",
        includes_tests: ["Add tests"]
      }
    })
  );
  return root;
}

describe("SkillRunner", () => {
  it("runs pr-review and writes a trace id", async () => {
    const root = await createProjectFixture();
    const runner = new SkillRunner({
      registry: new FileSkillRegistry({ rootDir: root }),
      rootDir: root
    });

    const result = await runner.run("pr-review", {
      version: "0.1.0",
      client: "api",
      inputs: {
        repo: "checkout",
        diff: "diff --git a/src/pay.ts b/src/pay.ts\\n+export function pay() { return charge(); }",
        risk_level: "medium"
      }
    });

    expect(result.skill_id).toBe("pr-review");
    expect(result.version).toBe("0.1.0");
    expect(result.trace_id).toMatch(/^trace_/);
    expect(result.result.deployment_risk).toBe("medium");
    expect(result.result.test_recommendations.join(" ")).toContain("Add tests");
  });

  it("rejects version mismatches", async () => {
    const root = await createProjectFixture();
    const runner = new SkillRunner({
      registry: new FileSkillRegistry({ rootDir: root }),
      rootDir: root
    });

    await expect(
      runner.run("pr-review", {
        version: "9.9.9",
        client: "api",
        inputs: { repo: "checkout", diff: "diff", risk_level: "low" }
      })
    ).rejects.toThrow("Unsupported version 9.9.9 for pr-review");
  });

  it("evaluates examples", async () => {
    const root = await createProjectFixture();
    const runner = new SkillRunner({
      registry: new FileSkillRegistry({ rootDir: root }),
      rootDir: root
    });

    const result = await runner.evaluate("pr-review");

    expect(result.skill_id).toBe("pr-review");
    expect(result.passed).toBe(true);
    expect(result.cases[0]).toMatchObject({ name: "missing tests", passed: true });
  });
});
```

- [ ] **Step 2: Run runner tests and verify failure**

Run:

```bash
npm test -- packages/skill-runner/test/runner.test.ts
```

Expected: FAIL because `SkillRunner` and PR Review logic do not exist.

- [ ] **Step 3: Add the real PR Review skill files**

Create `skills/pr-review/skill.yaml`:

```yaml
id: pr-review
name: Pull Request Review
version: 0.1.0
description: Reviews code changes for correctness, maintainability, tests, and risk.
instructions: instructions.md
inputs:
  - name: repo
    type: string
    required: true
  - name: diff
    type: string
    required: true
  - name: risk_level
    type: enum
    required: true
    values: [low, medium, high]
outputs:
  type: object
  schema:
    summary: string
    blocking_issues: array
    non_blocking_suggestions: array
    test_recommendations: array
    deployment_risk: string
tools: []
guardrails:
  require_citations: false
  no_secret_exfiltration: true
  confirm_before_write: true
targets:
  openai:
    expose_as_mcp_tool: true
  claude:
    package_as_skill: true
  cursor:
    expose_as_rule_and_mcp_tool: true
examples:
  - examples/basic-risk.json
  - examples/missing-tests.json
```

Create `skills/pr-review/instructions.md`:

```md
# Pull Request Review

Review code changes as a senior engineer.

Focus on correctness, edge cases, tests, deployment risk, and maintainability. Keep the result structured. Do not rewrite entire files. Do not request secrets or private credentials. If the diff includes risky write paths, authentication changes, payment logic, or missing tests, surface that risk clearly.
```

Create `skills/pr-review/examples/basic-risk.json`:

```json
{
  "name": "basic risk",
  "inputs": {
    "repo": "checkout-service",
    "diff": "diff --git a/src/cart.ts b/src/cart.ts\n+export function formatCart(items: Item[]) {\n+  return items.map(item => item.name).join(', ');\n+}",
    "risk_level": "low"
  },
  "expected": {
    "deployment_risk": "medium",
    "includes_suggestions": ["Validate empty inputs"],
    "includes_tests": ["Add tests"]
  }
}
```

Create `skills/pr-review/examples/missing-tests.json`:

```json
{
  "name": "missing tests",
  "inputs": {
    "repo": "checkout-service",
    "diff": "diff --git a/src/payment.ts b/src/payment.ts\n+export async function authorizePayment(card: Card) {\n+  return paymentGateway.charge(card);\n+}",
    "risk_level": "medium"
  },
  "expected": {
    "deployment_risk": "high",
    "includes_suggestions": ["Payment or authorization logic changed"],
    "includes_tests": ["Add tests"]
  }
}
```

- [ ] **Step 4: Implement deterministic PR Review logic**

Create `packages/skill-runner/src/prReview.ts`:

```ts
import { type PrReviewResult, prReviewResultSchema } from "@llm-skills-poc/skill-spec";

export interface PrReviewInputs {
  repo: string;
  diff: string;
  risk_level: "low" | "medium" | "high";
}

export function parsePrReviewInputs(inputs: Record<string, unknown>): PrReviewInputs {
  const repo = inputs.repo;
  const diff = inputs.diff;
  const riskLevel = inputs.risk_level;

  if (typeof repo !== "string" || repo.length === 0) {
    throw new Error("Missing required string input: repo");
  }
  if (typeof diff !== "string" || diff.length === 0) {
    throw new Error("Missing required string input: diff");
  }
  if (riskLevel !== "low" && riskLevel !== "medium" && riskLevel !== "high") {
    throw new Error("Missing required enum input: risk_level");
  }

  return { repo, diff, risk_level: riskLevel };
}

export function runPrReview(inputs: PrReviewInputs): PrReviewResult {
  const lowerDiff = inputs.diff.toLowerCase();
  const blockingIssues: string[] = [];
  const suggestions: string[] = [];
  const tests: string[] = [];

  if (lowerDiff.includes("password") || lowerDiff.includes("secret") || lowerDiff.includes("api_key")) {
    blockingIssues.push("Potential secret-handling change detected; verify no credentials are introduced or exposed.");
  }

  if (lowerDiff.includes("payment") || lowerDiff.includes("charge") || lowerDiff.includes("authorize")) {
    suggestions.push("Payment or authorization logic changed; review failure handling and rollback behavior.");
  }

  if (!lowerDiff.includes("test") && !lowerDiff.includes("spec")) {
    tests.push("Add tests that cover the changed behavior and at least one edge case.");
  }

  if (lowerDiff.includes("delete") || lowerDiff.includes("drop table") || lowerDiff.includes("truncate")) {
    suggestions.push("Destructive data operation detected; confirm safeguards and recovery path.");
  }

  if (suggestions.length === 0) {
    suggestions.push("Validate empty inputs and boundary conditions for the changed code path.");
  }

  if (tests.length === 0) {
    tests.push("Run the existing focused test suite for the touched module.");
  }

  const deploymentRisk = computeDeploymentRisk(inputs.risk_level, blockingIssues, suggestions, tests);

  return prReviewResultSchema.parse({
    summary: `Reviewed ${inputs.repo}. The change has ${deploymentRisk} deployment risk based on the supplied diff.`,
    blocking_issues: blockingIssues,
    non_blocking_suggestions: suggestions,
    test_recommendations: tests,
    deployment_risk: deploymentRisk
  });
}

function computeDeploymentRisk(
  requestedRisk: PrReviewInputs["risk_level"],
  blockingIssues: string[],
  suggestions: string[],
  tests: string[]
): PrReviewResult["deployment_risk"] {
  if (blockingIssues.length > 0) {
    return "high";
  }
  if (requestedRisk === "high") {
    return "high";
  }
  if (suggestions.some((suggestion) => suggestion.toLowerCase().includes("payment"))) {
    return "high";
  }
  if (requestedRisk === "medium" || tests.some((test) => test.includes("Add tests"))) {
    return "medium";
  }
  return "low";
}
```

- [ ] **Step 5: Implement runner orchestration**

Create `packages/skill-runner/src/runner.ts`:

```ts
import { mkdir, readFile, writeFile } from "node:fs/promises";
import { randomUUID } from "node:crypto";
import { join } from "node:path";
import {
  type SkillEvaluationResult,
  type SkillRunRequest,
  type SkillRunResult,
  skillEvaluationResultSchema,
  skillRunRequestSchema,
  skillRunResultSchema
} from "@llm-skills-poc/skill-spec";
import { resolveDataDir } from "./paths.js";
import { FileSkillRegistry } from "./registry.js";
import { parsePrReviewInputs, runPrReview } from "./prReview.js";

export interface SkillRunnerOptions {
  registry?: FileSkillRegistry;
  rootDir?: string;
  dataDir?: string;
}

export class SkillRunner {
  private readonly registry: FileSkillRegistry;
  private readonly dataDir: string;

  constructor(options: SkillRunnerOptions = {}) {
    this.registry = options.registry ?? new FileSkillRegistry({ rootDir: options.rootDir });
    this.dataDir = options.dataDir ?? resolveDataDir(options.rootDir);
  }

  async run(skillId: string, request: SkillRunRequest): Promise<SkillRunResult> {
    const parsedRequest = skillRunRequestSchema.parse(request);
    const skill = await this.registry.getSkill(skillId);

    if (parsedRequest.version && parsedRequest.version !== skill.manifest.version) {
      throw new Error(`Unsupported version ${parsedRequest.version} for ${skillId}`);
    }

    const result = this.runLoadedSkill(skill.manifest.id, parsedRequest.inputs);
    const traceId = `trace_${randomUUID()}`;
    const response = skillRunResultSchema.parse({
      skill_id: skill.manifest.id,
      version: skill.manifest.version,
      result,
      trace_id: traceId
    });

    await this.writeTrace(skill.manifest.id, traceId, {
      request: parsedRequest,
      response,
      created_at: new Date().toISOString()
    });

    return response;
  }

  async evaluate(skillId: string): Promise<SkillEvaluationResult> {
    const skill = await this.registry.getSkill(skillId);
    const cases = [];

    for (const example of skill.examples) {
      const result = await this.run(skillId, {
        client: "internal",
        version: skill.manifest.version,
        inputs: example.inputs
      });
      const failures: string[] = [];

      if (example.expected.deployment_risk && result.result.deployment_risk !== example.expected.deployment_risk) {
        failures.push(
          `Expected deployment_risk ${example.expected.deployment_risk}, got ${result.result.deployment_risk}`
        );
      }

      for (const phrase of example.expected.includes_suggestions) {
        if (!result.result.non_blocking_suggestions.join(" ").includes(phrase)) {
          failures.push(`Expected suggestion containing: ${phrase}`);
        }
      }

      for (const phrase of example.expected.includes_tests) {
        if (!result.result.test_recommendations.join(" ").includes(phrase)) {
          failures.push(`Expected test recommendation containing: ${phrase}`);
        }
      }

      cases.push({ name: example.name, passed: failures.length === 0, failures });
    }

    return skillEvaluationResultSchema.parse({
      skill_id: skill.manifest.id,
      version: skill.manifest.version,
      passed: cases.every((testCase) => testCase.passed),
      cases
    });
  }

  async listAudit(skillId: string): Promise<unknown[]> {
    const auditFile = join(this.dataDir, "traces", `${skillId}.jsonl`);
    try {
      const raw = await readFile(auditFile, "utf8");
      return raw
        .trim()
        .split("\\n")
        .filter(Boolean)
        .map((line) => JSON.parse(line));
    } catch (error) {
      if (error instanceof Error && "code" in error && error.code === "ENOENT") {
        return [];
      }
      throw error;
    }
  }

  private runLoadedSkill(skillId: string, inputs: Record<string, unknown>) {
    if (skillId !== "pr-review") {
      throw new Error(`No deterministic runner registered for ${skillId}`);
    }
    return runPrReview(parsePrReviewInputs(inputs));
  }

  private async writeTrace(skillId: string, traceId: string, payload: unknown): Promise<void> {
    const traceDir = join(this.dataDir, "traces");
    await mkdir(traceDir, { recursive: true });
    await writeFile(join(traceDir, `${skillId}.jsonl`), `${JSON.stringify({ trace_id: traceId, ...payload })}\\n`, {
      flag: "a"
    });
  }
}
```

- [ ] **Step 6: Export runner modules**

Replace `packages/skill-runner/src/index.ts` with:

```ts
export * from "./paths.js";
export * from "./prReview.js";
export * from "./registry.js";
export * from "./runner.js";
```

- [ ] **Step 7: Run runner tests and verify pass**

Run:

```bash
npm test -- packages/skill-runner/test/runner.test.ts
```

Expected: PASS.

- [ ] **Step 8: Run registry tests against the real skill files**

Run:

```bash
npm test -- packages/skill-runner/test/registry.test.ts
```

Expected: PASS.

- [ ] **Step 9: Commit runner and skill package**

Run:

```bash
git add packages/skill-runner skills/pr-review
git commit -m "feat: add pr review skill runner"
```

Expected: commit succeeds.

## Task 5: HTTP Registry API

**Files:**
- Create: `apps/api/src/app.ts`
- Create: `apps/api/src/index.ts`
- Create: `apps/api/test/api.test.ts`

- [ ] **Step 1: Write failing API integration tests**

Create `apps/api/test/api.test.ts`:

```ts
import request from "supertest";
import { describe, expect, it } from "vitest";
import { createApp } from "../src/app.js";

describe("Registry API", () => {
  const app = createApp();

  it("reports health", async () => {
    const response = await request(app).get("/healthz").expect(200);
    expect(response.body).toEqual({ ok: true });
  });

  it("lists skills", async () => {
    const response = await request(app).get("/skills").expect(200);
    expect(response.body.skills[0]).toMatchObject({
      id: "pr-review",
      version: "0.1.0"
    });
  });

  it("gets one skill", async () => {
    const response = await request(app).get("/skills/pr-review").expect(200);
    expect(response.body.skill.manifest.id).toBe("pr-review");
    expect(response.body.skill.examples).toHaveLength(2);
  });

  it("runs one skill", async () => {
    const response = await request(app)
      .post("/skills/pr-review/run")
      .send({
        client: "api",
        version: "0.1.0",
        inputs: {
          repo: "checkout-service",
          diff: "diff --git a/src/payment.ts b/src/payment.ts\n+export function charge() { return payment.charge(); }",
          risk_level: "medium"
        }
      })
      .expect(200);

    expect(response.body.skill_id).toBe("pr-review");
    expect(response.body.result.deployment_risk).toBe("high");
    expect(response.body.trace_id).toMatch(/^trace_/);
  });

  it("evaluates one skill", async () => {
    const response = await request(app).post("/skills/pr-review/evaluate").send({}).expect(200);
    expect(response.body.skill_id).toBe("pr-review");
    expect(response.body.passed).toBe(true);
  });

  it("returns audit events", async () => {
    const response = await request(app).get("/skills/pr-review/audit").expect(200);
    expect(Array.isArray(response.body.events)).toBe(true);
  });
});
```

- [ ] **Step 2: Run API tests and verify failure**

Run:

```bash
npm test -- apps/api/test/api.test.ts
```

Expected: FAIL because API app files do not exist.

- [ ] **Step 3: Implement Express app factory**

Create `apps/api/src/app.ts`:

```ts
import express, { type ErrorRequestHandler } from "express";
import { FileSkillRegistry, SkillRunner } from "@llm-skills-poc/skill-runner";

export interface CreateAppOptions {
  rootDir?: string;
}

export function createApp(options: CreateAppOptions = {}) {
  const app = express();
  const registry = new FileSkillRegistry({ rootDir: options.rootDir });
  const runner = new SkillRunner({ registry, rootDir: options.rootDir });

  app.use(express.json({ limit: "1mb" }));

  app.get("/healthz", (_req, res) => {
    res.json({ ok: true });
  });

  app.get("/skills", async (_req, res, next) => {
    try {
      const skills = await registry.listSkills();
      res.json({ skills });
    } catch (error) {
      next(error);
    }
  });

  app.get("/skills/:skillId", async (req, res, next) => {
    try {
      const skill = await registry.getSkill(req.params.skillId);
      res.json({ skill });
    } catch (error) {
      next(error);
    }
  });

  app.post("/skills/:skillId/run", async (req, res, next) => {
    try {
      const result = await runner.run(req.params.skillId, req.body);
      res.json(result);
    } catch (error) {
      next(error);
    }
  });

  app.post("/skills/:skillId/evaluate", async (req, res, next) => {
    try {
      const result = await runner.evaluate(req.params.skillId);
      res.json(result);
    } catch (error) {
      next(error);
    }
  });

  app.get("/skills/:skillId/audit", async (req, res, next) => {
    try {
      const events = await runner.listAudit(req.params.skillId);
      res.json({ events });
    } catch (error) {
      next(error);
    }
  });

  const errorHandler: ErrorRequestHandler = (error, _req, res, _next) => {
    const message = error instanceof Error ? error.message : "Unknown error";
    const status = message.includes("not found") ? 404 : 400;
    res.status(status).json({ error: message });
  };
  app.use(errorHandler);

  return app;
}
```

- [ ] **Step 4: Implement API entrypoint**

Create `apps/api/src/index.ts`:

```ts
import { createApp } from "./app.js";

const port = Number(process.env.PORT ?? "3000");
const app = createApp();

app.listen(port, () => {
  console.log(`Shared Skills API listening on http://localhost:${port}`);
});
```

- [ ] **Step 5: Run API tests and verify pass**

Run:

```bash
npm test -- apps/api/test/api.test.ts
```

Expected: PASS.

- [ ] **Step 6: Build API app**

Run:

```bash
npm run build --workspace @llm-skills-poc/api
```

Expected: PASS.

- [ ] **Step 7: Commit API app**

Run:

```bash
git add apps/api packages/skill-runner skills/pr-review
git commit -m "feat: add registry HTTP API"
```

Expected: commit succeeds.

## Task 6: MCP Server

**Files:**
- Create: `apps/mcp-server/src/server.ts`
- Create: `apps/mcp-server/src/index.ts`
- Create: `apps/mcp-server/test/mcp-server.test.ts`

- [ ] **Step 1: Write failing MCP server tests**

Create `apps/mcp-server/test/mcp-server.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { createSkillsMcpServer, listSkillsPayload, runSkillPayload } from "../src/server.js";

describe("MCP server adapter", () => {
  it("creates a server object", () => {
    const server = createSkillsMcpServer();
    expect(server).toBeDefined();
  });

  it("lists skills through shared registry helper", async () => {
    const payload = await listSkillsPayload();
    expect(payload.skills[0]).toMatchObject({ id: "pr-review", version: "0.1.0" });
  });

  it("runs skills through shared runner helper", async () => {
    const payload = await runSkillPayload({
      skill_id: "pr-review",
      version: "0.1.0",
      client: "mcp",
      inputs: {
        repo: "checkout-service",
        diff: "diff --git a/src/payment.ts b/src/payment.ts\n+export function charge() { return payment.charge(); }",
        risk_level: "medium"
      }
    });

    expect(payload.skill_id).toBe("pr-review");
    expect(payload.result.deployment_risk).toBe("high");
  });
});
```

- [ ] **Step 2: Run MCP tests and verify failure**

Run:

```bash
npm test -- apps/mcp-server/test/mcp-server.test.ts
```

Expected: FAIL because MCP server files do not exist.

- [ ] **Step 3: Implement MCP server factory and tool helpers**

Create `apps/mcp-server/src/server.ts`:

```ts
import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { z } from "zod";
import { FileSkillRegistry, SkillRunner } from "@llm-skills-poc/skill-runner";

const registry = new FileSkillRegistry();
const runner = new SkillRunner({ registry });

export async function listSkillsPayload() {
  return { skills: await registry.listSkills() };
}

export async function getSkillPayload(skillId: string) {
  return { skill: await registry.getSkill(skillId) };
}

export async function runSkillPayload(input: {
  skill_id: string;
  version?: string;
  client?: "openai" | "claude" | "cursor" | "internal" | "api" | "mcp";
  inputs: Record<string, unknown>;
}) {
  return runner.run(input.skill_id, {
    version: input.version,
    client: input.client ?? "mcp",
    inputs: input.inputs
  });
}

export async function evaluateSkillPayload(skillId: string) {
  return runner.evaluate(skillId);
}

export function createSkillsMcpServer() {
  const server = new McpServer({
    name: "shared-skills",
    version: "0.1.0"
  });

  server.registerTool(
    "list_skills",
    {
      title: "List Skills",
      description: "List validated skills in the Shared Skills registry.",
      inputSchema: {}
    },
    async () => ({
      content: [{ type: "text", text: JSON.stringify(await listSkillsPayload(), null, 2) }]
    })
  );

  server.registerTool(
    "get_skill",
    {
      title: "Get Skill",
      description: "Get one validated skill manifest, instructions, and examples.",
      inputSchema: { skill_id: z.string().min(1) }
    },
    async ({ skill_id }) => ({
      content: [{ type: "text", text: JSON.stringify(await getSkillPayload(skill_id), null, 2) }]
    })
  );

  server.registerTool(
    "run_skill",
    {
      title: "Run Skill",
      description: "Run a skill through the shared deterministic runner.",
      inputSchema: {
        skill_id: z.string().min(1),
        version: z.string().optional(),
        client: z.enum(["openai", "claude", "cursor", "internal", "api", "mcp"]).optional(),
        inputs: z.record(z.string(), z.unknown())
      }
    },
    async (input) => ({
      content: [{ type: "text", text: JSON.stringify(await runSkillPayload(input), null, 2) }]
    })
  );

  server.registerTool(
    "evaluate_skill",
    {
      title: "Evaluate Skill",
      description: "Run local evaluation examples for one skill.",
      inputSchema: { skill_id: z.string().min(1) }
    },
    async ({ skill_id }) => ({
      content: [{ type: "text", text: JSON.stringify(await evaluateSkillPayload(skill_id), null, 2) }]
    })
  );

  return server;
}
```

- [ ] **Step 4: Implement stdio entrypoint**

Create `apps/mcp-server/src/index.ts`:

```ts
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
import { createSkillsMcpServer } from "./server.js";

const server = createSkillsMcpServer();
const transport = new StdioServerTransport();
await server.connect(transport);
```

- [ ] **Step 5: Run MCP tests and verify pass**

Run:

```bash
npm test -- apps/mcp-server/test/mcp-server.test.ts
```

Expected: PASS.

- [ ] **Step 6: Build MCP app**

Run:

```bash
npm run build --workspace @llm-skills-poc/mcp-server
```

Expected: PASS.

- [ ] **Step 7: Commit MCP server**

Run:

```bash
git add apps/mcp-server
git commit -m "feat: add shared skills MCP server"
```

Expected: commit succeeds.

## Task 7: Docker And README

**Files:**
- Create: `Dockerfile`
- Create: `docker-compose.yml`
- Create: `README.md`

- [ ] **Step 1: Create Dockerfile**

Create `Dockerfile`:

```dockerfile
FROM node:22-bookworm-slim AS deps
WORKDIR /app
COPY package.json package-lock.json ./
COPY apps/api/package.json apps/api/package.json
COPY apps/mcp-server/package.json apps/mcp-server/package.json
COPY packages/skill-spec/package.json packages/skill-spec/package.json
COPY packages/skill-runner/package.json packages/skill-runner/package.json
RUN npm ci

FROM deps AS build
COPY tsconfig.base.json vitest.config.ts ./
COPY apps apps
COPY packages packages
COPY skills skills
RUN npm run build

FROM node:22-bookworm-slim AS runtime
WORKDIR /app
ENV NODE_ENV=production
ENV SKILLS_PROJECT_ROOT=/app
COPY package.json package-lock.json ./
COPY apps/api/package.json apps/api/package.json
COPY apps/mcp-server/package.json apps/mcp-server/package.json
COPY packages/skill-spec/package.json packages/skill-spec/package.json
COPY packages/skill-runner/package.json packages/skill-runner/package.json
RUN npm ci --omit=dev
COPY --from=build /app/apps apps
COPY --from=build /app/packages packages
COPY skills skills
EXPOSE 3000
CMD ["node", "apps/api/dist/index.js"]
```

- [ ] **Step 2: Create Docker Compose file**

Create `docker-compose.yml`:

```yaml
services:
  api:
    build: .
    environment:
      PORT: "3000"
      SKILLS_PROJECT_ROOT: /app
    ports:
      - "3000:3000"
    volumes:
      - ./skills:/app/skills:ro
      - ./.data:/app/.data
```

- [ ] **Step 3: Create README**

Create `README.md`:

````md
# Shared Skills API Prototype

This repo is a Phase 1 prototype for defining a reusable AI assistant skill once and exposing it through both an HTTP API and an MCP server.

## Stack

- TypeScript and Node
- npm workspaces
- Express Registry API
- MCP stdio server
- File-backed skills under `skills/`
- File-backed traces under `.data/`

## Install

```bash
npm install
```

## Test

```bash
npm test
```

## Build

```bash
npm run build
```

## Run API Locally

```bash
npm run dev:api
```

Then call:

```bash
curl http://localhost:3000/healthz
curl http://localhost:3000/skills
curl -X POST http://localhost:3000/skills/pr-review/run \
  -H 'Content-Type: application/json' \
  -d '{
    "client": "api",
    "version": "0.1.0",
    "inputs": {
      "repo": "checkout-service",
      "diff": "diff --git a/src/payment.ts b/src/payment.ts\n+export function charge() { return payment.charge(); }",
      "risk_level": "medium"
    }
  }'
```

## Run MCP Server Locally

```bash
npm run dev:mcp
```

MCP-compatible clients can launch the server with:

```bash
node apps/mcp-server/dist/index.js
```

Available tools:

- `list_skills`
- `get_skill`
- `run_skill`
- `evaluate_skill`

## Docker

```bash
docker compose up --build
```

Then call:

```bash
curl http://localhost:3000/healthz
```

To run the MCP server from the built image:

```bash
docker compose build
docker run --rm -i llm-skills-poc-api node apps/mcp-server/dist/index.js
```
````

- [ ] **Step 4: Build Docker image**

Run:

```bash
docker compose build
```

Expected: PASS and an API image is built.

- [ ] **Step 5: Run Docker API smoke test**

Run:

```bash
docker compose up
```

In another terminal, run:

```bash
curl http://localhost:3000/healthz
```

Expected response:

```json
{"ok":true}
```

- [ ] **Step 6: Stop Docker Compose**

Stop the foreground compose process with `Ctrl-C`, or run this if detached:

```bash
docker compose down
```

Expected: containers stop cleanly.

- [ ] **Step 7: Commit Docker and docs**

Run:

```bash
git add Dockerfile docker-compose.yml README.md
git commit -m "docs: add Docker runbook"
```

Expected: commit succeeds.

## Task 8: Final Verification

**Files:**
- Read: all implemented files
- Modify: only files needed to fix verification failures

- [ ] **Step 1: Run full test suite**

Run:

```bash
npm test
```

Expected: all tests pass.

- [ ] **Step 2: Run full TypeScript build**

Run:

```bash
npm run build
```

Expected: all workspace packages build.

- [ ] **Step 3: Run API smoke test locally**

Run:

```bash
npm run dev:api
```

In another terminal, run:

```bash
curl http://localhost:3000/skills/pr-review
```

Expected: JSON response includes `"id":"pr-review"` in `skill.manifest`.

- [ ] **Step 4: Stop local API**

Stop the foreground API process with `Ctrl-C`.

Expected: process exits cleanly.

- [ ] **Step 5: Run Docker smoke test**

Run:

```bash
docker compose up --build
```

In another terminal, run:

```bash
curl http://localhost:3000/healthz
```

Expected response:

```json
{"ok":true}
```

- [ ] **Step 6: Stop Docker Compose**

Stop the foreground compose process with `Ctrl-C`, or run this if detached:

```bash
docker compose down
```

Expected: containers stop cleanly.

- [ ] **Step 7: Inspect git status**

Run:

```bash
git status --short --branch
```

Expected: clean working tree on `main`.

- [ ] **Step 8: Push implementation branch**

Run:

```bash
git push
```

Expected: local commits are pushed to `origin/main`.
